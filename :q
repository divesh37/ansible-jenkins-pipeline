---
# tasks file for roles/unit_test_prana_api
    - name: Checking container status
      command: bash -lc "cd {{ project_path }} && docker-compose -f {{ composefile }} up -d"
      tags: prana_unit_test
    - command: bash -lc "cd {{ project_path }} && docker-compose -f {{ composefile }} ps -q api"
      register: container_id
      tags: prana_unit_test
    - command: "docker exec {{ container_id.stdout }} "npm run test""
      register: prana_unit_test_output
      tags: prana_unit_test
    - debug: msg={{ prana_unit_test_output }}
    
